This is an ongoing repository about constructing _Reduced Order Model_ (ROM) for CFD-fields.
The POD with interpolation methods are applied for building the ROM. 
The interpolation (i.e., surrogate) is achieved by RBF, GPR, and ANN.
